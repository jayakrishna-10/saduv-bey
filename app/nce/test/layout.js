// app/nce/test/layout.js
export const metadata = {
  title: 'NCE Mock Tests',
  description: 'Take mock tests for National Certification Examination with timer and comprehensive analysis',
};

export default function TestLayout({ children }) {
  return children;
}
